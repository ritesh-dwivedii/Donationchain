import express, { Router } from 'express';

const router: Router = express.Router();

// Add routes here if needed
// Currently, all campaign handling is done client-side with Web3

export default router; 